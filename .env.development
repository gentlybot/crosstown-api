# Local development only. No secrets here; the sandbox overrides these.
PORT=3200
PGPORT=5440
PGUSER=crosstown
PGPASSWORD=crosstown
REDIS_URL=redis://localhost:6390/0
SMTP_HOST=localhost
SMTP_PORT=1026
CORS_ORIGINS=http://localhost:5200,http://localhost:5300
PORTAL_URL=http://localhost:5200

Hi <%= @merchant.contact_name.presence || @merchant.business_name %>,

<%= @batch.name %> finished importing.

  Orders ready:     <%= @batch.ready_count %>
  Need attention:   <%= @batch.problem_count %>
  Delivery date:    <%= @batch.delivery_date.strftime("%A %-d %B") %>

<% if @batch.problem_count.positive? -%>
Fix the flagged rows in the portal before the cutoff so they can go out with the rest.
<% else -%>
Nothing to fix. The batch will be routed after your <%= @merchant.cutoff_time %> cutoff.
<% end -%>

<%= @portal_url %>/merchant/batches/<%= @batch.id %>

Crosstown
